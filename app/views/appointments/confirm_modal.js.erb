modal = $("<%= j render('confirm_modal') %>");

if ($(".modal.in").length > 0)
{
  $(".modal.in").removeClass("fade");
  modal.removeClass("fade");
}

$(".modal.in").modal('hide');
modal.appendTo($("body")).modal('show');