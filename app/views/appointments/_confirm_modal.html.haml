#modal-window.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        %button{ :type => "button", class: "close", "data-dismiss" => "modal" }
          %span{ "aria-hidden" => true }
            &times;
          %span{ class: "sr-only" }
            Close
        %h4{ class: "modal-title" }
          = "Book Now"
      .modal-body
        = form_for :charge do
          %h6
            Appointment Details
            %ul
              %li= "Duration: #{@appointment.lesson_duration} minutes"
              %li= "Start Time: #{@appointment.display_local_start_time}"
              %li= "Instructor: #{@appointment.instructor.full_name}"
              %li= "#{@appointment.display_price}"
      .modal-footer
        %script{ class: "stripe-button", :src => "https://checkout.stripe.com/checkout.js", "data-key" => "#{Rails.configuration.stripe[:publishable_key]}", "data-description" => "#{@appointment.appointment_category.name}", "data-amount" => "#{@appointment.price_in_cents}"}